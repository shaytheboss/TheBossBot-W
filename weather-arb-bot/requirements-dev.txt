# תלויות פיתוח/בדיקות בלבד — לא נכנסות לאימג' הפרודקשן
pytest>=8
pytest-asyncio>=0.23
aiosqlite>=0.19    # DB בזיכרון לבדיקות האינטגרציה של model_skill
ruff>=0.5        # linter — config in ruff.toml, must pass clean before commit
